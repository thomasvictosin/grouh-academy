'use client'

import { Ban, Search, UserRound, UserX } from 'lucide-react'
import { useMemo, useState } from 'react'
import { MentorBadge, MentorPage } from '@/components/MentorPage'

const initialInterns = [{ id: 1, name: 'Amaka Okafor', email: 'amaka@example.com', track: 'Software Development', cohort: 'Cohort A', progress: '78%', status: 'Active' }, { id: 2, name: 'Daniel Mensah', email: 'daniel@example.com', track: 'Product Design', cohort: 'Cohort A', progress: '61%', status: 'Active' }, { id: 3, name: 'Ifeoma Nwosu', email: 'ifeoma@example.com', track: 'Software Development', cohort: 'Cohort B', progress: '92%', status: 'Active' }, { id: 4, name: 'Tunde Bello', email: 'tunde@example.com', track: 'Data Analytics', cohort: 'Cohort B', progress: '34%', status: 'Suspended' }]
export default function MentorInternsPage() { const [interns, setInterns] = useState(initialInterns); const [query, setQuery] = useState(''); const visible = useMemo(() => interns.filter((intern) => `${intern.name} ${intern.email} ${intern.track}`.toLowerCase().includes(query.toLowerCase())), [interns, query]); const toggleStatus = (id: number) => setInterns((items) => items.map((item) => item.id === id ? { ...item, status: item.status === 'Suspended' ? 'Active' : 'Suspended' } : item)); return <MentorPage title="My Interns" description="Manage the interns assigned to your supervision, monitor progress, and suspend or restore access when necessary."><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2 text-sm font-bold text-[#1C1D52]"><UserRound className="h-4 w-4 text-[#5FBB46]" />24 of 30 supervision slots used</div><label className="flex items-center gap-2 rounded-full bg-[#f3f6fb] px-3 py-2 text-[10px] text-slate-400"><Search className="h-3.5 w-3.5" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search interns" className="bg-transparent outline-none" /></label></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead className="text-[10px] uppercase tracking-[0.1em] text-slate-400"><tr><th className="pb-3">Intern</th><th className="pb-3">Track</th><th className="pb-3">Cohort</th><th className="pb-3">Progress</th><th className="pb-3">Status</th><th className="pb-3">Actions</th></tr></thead><tbody className="text-xs">{visible.map((intern) => <tr key={intern.id} className="border-t border-slate-100"><td className="py-4"><p className="font-bold text-[#1C1D52]">{intern.name}</p><p className="mt-1 text-[10px] text-slate-500">{intern.email}</p></td><td className="py-4 text-slate-500">{intern.track}</td><td className="py-4 text-slate-500">{intern.cohort}</td><td className="py-4 font-semibold text-[#1C1D52]">{intern.progress}</td><td className="py-4"><MentorBadge tone={intern.status === 'Active' ? 'green' : 'red'}>{intern.status}</MentorBadge></td><td className="py-4"><div className="flex gap-2"><button type="button" className="rounded-lg border border-slate-200 px-2.5 py-2 text-[10px] font-bold text-blue-600">View</button><button type="button" onClick={() => toggleStatus(intern.id)} className="inline-flex items-center gap-1 rounded-lg border border-slate-200 px-2.5 py-2 text-[10px] font-bold text-red-600">{intern.status === 'Suspended' ? <UserRound className="h-3 w-3" /> : <Ban className="h-3 w-3" />}{intern.status === 'Suspended' ? 'Restore' : 'Suspend'}</button><button type="button" onClick={() => setInterns((items) => items.filter((item) => item.id !== intern.id))} className="rounded-lg border border-red-100 px-2.5 py-2 text-red-500" aria-label={`Remove ${intern.name}`}><UserX className="h-3 w-3" /></button></div></td></tr>)}</tbody></table></div></section></MentorPage> }
