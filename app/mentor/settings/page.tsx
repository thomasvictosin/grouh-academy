'use client'

import { Bell, Check, Lock, Save, Settings2, Users } from 'lucide-react'
import { useState } from 'react'
import { MentorPage } from '@/components/MentorPage'
export default function MentorSettingsPage() { const [settings, setSettings] = useState({ questions: true, assessments: true, taskSubmissions: true, capacity: '30' }); const [saved, setSaved] = useState(false); const toggle = (key: 'questions' | 'assessments' | 'taskSubmissions') => setSettings((current) => ({ ...current, [key]: !current[key] })); return <MentorPage title="Settings" description="Configure mentor notifications, supervision capacity, and account security."><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-7"><Heading icon={<Settings2 className="h-5 w-5" />} title="Supervision preferences" text="Set the maximum number of interns you can actively supervise." /><label className="mt-5 block max-w-xs text-xs font-bold text-[#1C1D52]">Supervision capacity<select value={settings.capacity} onChange={(event) => setSettings({ ...settings, capacity: event.target.value })} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-xs font-normal"><option value="20">20 interns</option><option value="30">30 interns</option><option value="40">40 interns</option></select></label></section><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-7"><Heading icon={<Bell className="h-5 w-5" />} title="Mentor notifications" text="Choose which supervision activity should reach you." /><div className="mt-5 divide-y divide-slate-100">{[['questions', 'Intern questions', 'Receive alerts when supervised interns send messages.'], ['assessments', 'Assessment submissions', 'Receive alerts when an assessment needs your review.'], ['taskSubmissions', 'Task submissions', 'Receive alerts when an intern submits assigned work.']].map(([key, title, text]) => <div key={key} className="flex items-center justify-between gap-4 py-4"><div><p className="text-xs font-bold text-[#1C1D52]">{title}</p><p className="mt-1 text-[10px] text-slate-500">{text}</p></div><button type="button" onClick={() => toggle(key as 'questions' | 'assessments' | 'taskSubmissions')} className={`relative h-6 w-11 shrink-0 rounded-full ${settings[key as keyof typeof settings] ? 'bg-[#5FBB46]' : 'bg-slate-200'}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white ${settings[key as keyof typeof settings] ? 'left-6' : 'left-1'}`} /></button></div>)}</div></section><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-7"><Heading icon={<Users className="h-5 w-5" />} title="Availability" text="Let coordinators know when you are available for mentor sessions." /><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Available days" value="Monday, Wednesday, Friday" /><Field label="Available hours" value="2:00 PM - 4:00 PM" /></div></section><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-7"><Heading icon={<Lock className="h-5 w-5" />} title="Security" text="Keep your mentor account protected." /><button type="button" className="mt-5 rounded-lg border border-slate-200 px-4 py-2.5 text-xs font-bold text-[#1C1D52]">Change password</button></section><div className="flex items-center gap-3"><button type="button" onClick={() => { setSaved(true); window.setTimeout(() => setSaved(false), 2500) }} className="inline-flex items-center gap-2 rounded-lg bg-[#5FBB46] px-4 py-2.5 text-xs font-bold text-[#14204f]"><Save className="h-4 w-4" />Save settings</button>{saved && <span className="inline-flex items-center gap-1 text-xs font-semibold text-[#397d3a]"><Check className="h-4 w-4" />Settings saved</span>}</div></MentorPage> }
function Heading({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[#dceeff] text-blue-600">{icon}</span><div><h2 className="text-sm font-bold text-[#1C1D52]">{title}</h2><p className="mt-1 text-[10px] text-slate-500">{text}</p></div></div> }
function Field({ label, value }: { label: string; value: string }) { return <label className="block text-xs font-bold text-[#1C1D52]">{label}<input defaultValue={value} className="mt-2 h-10 w-full rounded-lg border border-slate-200 px-3 text-xs font-normal outline-none focus:border-blue-400" /></label> }
