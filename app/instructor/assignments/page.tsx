'use client'

import { CheckCircle2, Clock3, Filter } from 'lucide-react'
import { useState } from 'react'
import { InstructorPage, StatusBadge } from '@/components/InstructorPage'
const submissions = [{ student: 'Amaka Okafor', assignment: 'Product discovery brief', course: 'AI Product Design Bootcamp', submitted: '2 hours ago', status: 'Pending grade' }, { student: 'Daniel Mensah', assignment: 'Design system audit', course: 'Design Systems for Product Teams', submitted: 'Yesterday', status: 'Pending grade' }, { student: 'Ifeoma Nwosu', assignment: 'Prototype walkthrough', course: 'Rapid Prototyping Workshop', submitted: 'Sep 5', status: 'Graded' }]
export default function InstructorAssignmentsPage() { const [filter, setFilter] = useState('All'); const visible = submissions.filter((item) => filter === 'All' || item.status === filter); return <InstructorPage title="Assignments & Grading" description="Review practical submissions, provide feedback, and record grades for your learners."><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><h2 className="text-sm font-bold text-[#1C1D52]">Submission inbox</h2><label className="flex items-center gap-2 text-[10px] text-slate-500"><Filter className="h-3.5 w-3.5" /><select value={filter} onChange={(event) => setFilter(event.target.value)} className="rounded-lg border border-slate-200 px-2 py-2 outline-none"><option>All</option><option>Pending grade</option><option>Graded</option></select></label></div><div className="mt-5 space-y-3">{visible.map((item) => <article key={`${item.student}-${item.assignment}`} className="flex flex-col gap-3 rounded-xl border border-slate-200 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold text-[#1C1D52]">{item.assignment}</p><p className="mt-1 text-[10px] text-slate-500">{item.student} · {item.course} · {item.submitted}</p></div><div className="flex items-center gap-3">{item.status === 'Pending grade' ? <Clock3 className="h-4 w-4 text-amber-500" /> : <CheckCircle2 className="h-4 w-4 text-[#5FBB46]" />}<StatusBadge tone={item.status === 'Graded' ? 'green' : 'amber'}>{item.status}</StatusBadge><button type="button" className="text-[10px] font-bold text-blue-600">{item.status === 'Graded' ? 'View grade' : 'Grade now'}</button></div></article>)}</div></section></InstructorPage> }
