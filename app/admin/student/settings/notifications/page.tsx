import AdminShell from '@/components/AdminShell'
import AdminSettings, { SettingsActions, SettingsToggle } from '@/components/AdminSettings'

export default function NotificationSettingsPage() {
  return <AdminShell workspace="student"><AdminSettings active="notifications" title="Notification Settings" description="Choose which platform events should send notifications to administrators and students."><form className="pt-5"><h3 className="text-sm font-bold text-[#1C1D52]">Email Notifications</h3><div className="mt-3 divide-y divide-slate-100"><SettingsToggle label="New student registration" description="Notify administrators when a student creates an account." defaultChecked /><SettingsToggle label="Course enrollment" description="Notify students when they are enrolled in a course." defaultChecked /><SettingsToggle label="Payment received" description="Notify administrators after a successful payment." defaultChecked /><SettingsToggle label="Course completion" description="Notify instructors when a student completes a course." defaultChecked /><SettingsToggle label="New support message" defaultChecked /></div><div className="mt-7 border-t border-slate-200 pt-5"><h3 className="text-sm font-bold text-[#1C1D52]">In-app Notifications</h3><div className="mt-3 divide-y divide-slate-100"><SettingsToggle label="Enable notification center" defaultChecked /><SettingsToggle label="Show unread notification badge" defaultChecked /></div></div><SettingsActions /></form></AdminSettings></AdminShell>
}
