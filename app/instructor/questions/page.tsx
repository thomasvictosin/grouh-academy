import { MessageCircleQuestion, Search } from 'lucide-react'
import { InstructorPage, StatusBadge } from '@/components/InstructorPage'
const questions = [{ name: 'Amaka Okafor', course: 'AI Product Design Bootcamp', question: 'How should I prioritize research findings?', time: '18 min ago', tone: 'amber' as const }, { name: 'Daniel Mensah', course: 'Design Systems for Product Teams', question: 'Can I submit the audit as a PDF?', time: '2 hours ago', tone: 'amber' as const }, { name: 'Ifeoma Nwosu', course: 'Rapid Prototyping Workshop', question: 'Which prototype fidelity should I use?', time: 'Yesterday', tone: 'green' as const }]
export default function InstructorQuestionsPage() { return <InstructorPage title="Questions & Answers" description="Respond to learner questions and keep useful explanations visible across your courses."><section className="rounded-2xl bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><h2 className="flex items-center gap-2 text-sm font-bold text-[#1C1D52]"><MessageCircleQuestion className="h-4 w-4 text-blue-500" />Learner questions</h2><label className="flex items-center gap-2 rounded-full bg-[#f3f6fb] px-3 py-2 text-[10px] text-slate-400"><Search className="h-3.5 w-3.5" /><input placeholder="Search questions" className="bg-transparent outline-none" /></label></div><div className="mt-5 space-y-3">{questions.map((item) => <article key={item.question} className="rounded-xl border border-slate-200 p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-bold text-[#1C1D52]">{item.question}</p><p className="mt-1 text-[10px] text-slate-500">{item.name} · {item.course} · {item.time}</p></div><StatusBadge tone={item.tone}>{item.tone === 'green' ? 'Answered' : 'Needs reply'}</StatusBadge></div>{item.tone === 'amber' && <button type="button" className="mt-4 rounded-lg bg-[#5FBB46] px-3 py-2 text-[10px] font-bold text-[#14204f]">Answer question</button>}</article>)}</div></section></InstructorPage> }
