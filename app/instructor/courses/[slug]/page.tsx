import { ArrowLeft, Edit3, FileText, Users } from 'lucide-react'
import Link from 'next/link'
import { InstructorPage, StatusBadge } from '@/components/InstructorPage'

export default async function InstructorCoursePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const title = slug.replaceAll('-', ' ').replace(/\b\w/g, (letter) => letter.toUpperCase()); return <InstructorPage title={title} description="Review your course structure, student enrollment, and submission status before making changes." action={{ label: 'Edit course', href: `/instructor/courses/${slug}/edit` }}><Link href="/instructor/courses" className="inline-flex items-center gap-2 text-xs font-bold text-[#1C1D52]"><ArrowLeft className="h-4 w-4" />Back to courses</Link><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-white p-5 shadow-sm"><Users className="h-5 w-5 text-blue-500" /><strong className="mt-3 block text-xl text-[#1C1D52]">248</strong><span className="text-xs text-slate-500">Enrolled students</span></div><div className="rounded-2xl bg-white p-5 shadow-sm"><FileText className="h-5 w-5 text-[#5FBB46]" /><strong className="mt-3 block text-xl text-[#1C1D52]">24</strong><span className="text-xs text-slate-500">Lessons</span></div><div className="rounded-2xl bg-white p-5 shadow-sm"><Edit3 className="h-5 w-5 text-amber-500" /><strong className="mt-3 block text-xl text-[#1C1D52]">In review</strong><span className="text-xs text-slate-500">Admin approval status</span></div></div><section className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-sm font-bold text-[#1C1D52]">Course status</h2><StatusBadge tone="amber">Pending admin review</StatusBadge></div><p className="mt-4 text-xs leading-5 text-slate-500">Your course content is available for editing. Publishing is controlled by the academy administrator.</p></section></InstructorPage> }
