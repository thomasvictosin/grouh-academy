import { ArrowLeft, BookOpen, CheckCircle2, Clock3, Users } from 'lucide-react'
import Link from 'next/link'
import { notFound } from 'next/navigation'
import EnrollButton from '@/components/EnrollButton'
import { getCourseBySlug } from '@/lib/course-data'

export default async function CoursePreviewPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
  const course = await getCourseBySlug(slug, true)
  if (!course) notFound()
  const lessonCount = course.modules.reduce((total, module) => total + module.lessons.length, 0)

  return <div className="w-full max-w-full space-y-5 overflow-x-hidden"><Link href="/student/explore-courses" className="inline-flex items-center gap-2 text-xs font-semibold text-[#1C1D52] hover:text-blue-600"><ArrowLeft className="h-4 w-4" />Back to Explore Courses</Link><section className="overflow-hidden rounded-2xl bg-[#1C1D52] text-white shadow-[0_12px_28px_rgba(28,29,82,0.16)]"><div className="grid gap-6 px-6 py-7 sm:px-8 lg:grid-cols-[1.2fr_0.8fr] lg:items-end lg:px-10 lg:py-9"><div><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#9be28a]">Published course</p><h1 className="mt-3 max-w-2xl text-3xl font-bold tracking-tight sm:text-4xl">{course.title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/75">{course.description ?? 'A practical course from Grouh Academy.'}</p><p className="mt-4 text-xs text-white/60">{course.category?.name ?? 'Academy course'} · Taught by {course.instructors[0]?.instructor.name ?? course.creator.name ?? 'Grouh Academy'}</p><div className="mt-6 flex flex-wrap gap-3"><EnrollButton slug={slug} /><span className="inline-flex items-center gap-2 rounded-lg border border-white/15 px-4 py-2.5 text-xs font-semibold text-white/80"><CheckCircle2 className="h-4 w-4 text-[#9be28a]" />{course.currency} {course.price}</span></div></div><div className="rounded-xl border border-white/10 bg-white/10 p-5"><p className="text-xs font-semibold text-white/80">Course details</p><div className="mt-4 grid grid-cols-2 gap-4 text-[10px] text-white/65"><span className="flex items-center gap-2"><Clock3 className="h-3.5 w-3.5" />{course.modules.length} modules</span><span className="flex items-center gap-2"><BookOpen className="h-3.5 w-3.5" />{lessonCount} lessons</span><span className="flex items-center gap-2"><Users className="h-3.5 w-3.5" />{course._count.enrollments} learners</span><span>{course._count.reviews} reviews</span></div></div></div></section><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-7"><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-[#5FBB46]">Course roadmap</p><h2 className="mt-1 text-xl font-bold text-[#1C1D52]">What you will learn</h2></div><p className="text-xs text-slate-500">{lessonCount} lessons across {course.modules.length} modules</p></div><div className="mt-5 space-y-3">{course.modules.map((module, index) => <details key={module.id} open={index === 0} className="group rounded-xl border border-slate-200 bg-[#f8fbff]"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 p-4 [&::-webkit-details-marker]:hidden"><div className="flex min-w-0 items-center gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-[#dceeff] text-xs font-bold text-blue-600">{String(index + 1).padStart(2, '0')}</span><div className="min-w-0"><h3 className="truncate text-sm font-bold text-[#1C1D52]">{module.title}</h3><p className="mt-1 text-[10px] text-slate-500">{module.description ?? 'Course module content'}</p></div></div><span className="shrink-0 text-[10px] font-semibold text-slate-500">{module.lessons.length} lessons</span></summary><div className="border-t border-slate-200 px-4 pb-4 pt-3 sm:pl-16"><div className="space-y-2">{module.lessons.map((lesson) => <div key={lesson.id} className="flex items-center gap-2 rounded-lg bg-white px-3 py-2.5 text-xs text-slate-600"><BookOpen className="h-3.5 w-3.5 text-[#5FBB46]" />{lesson.title}</div>)}</div></div></details>)}</div></section></div>
}
