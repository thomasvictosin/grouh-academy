'use client'

import { BookOpen, Edit3, Eye, Filter, Plus, Search, Send } from 'lucide-react'
import Link from 'next/link'
import { useMemo, useState } from 'react'
import { InstructorPage, StatusBadge } from '@/components/InstructorPage'

const courses = [{ slug: 'ai-product-design-bootcamp', title: 'AI Product Design Bootcamp', category: 'Design', students: 248, lessons: 24, updated: 'Today', status: 'In review', tone: 'amber' as const }, { slug: 'design-systems', title: 'Design Systems for Product Teams', category: 'Design', students: 186, lessons: 18, updated: 'Yesterday', status: 'Published', tone: 'green' as const }, { slug: 'rapid-prototyping', title: 'Rapid Prototyping Workshop', category: 'Development', students: 94, lessons: 10, updated: 'Sep 4, 2026', status: 'Draft', tone: 'blue' as const }]

export default function InstructorCoursesPage() {
	const [query, setQuery] = useState('')
	const [status, setStatus] = useState('All')
	const [category, setCategory] = useState('All')
	const visibleCourses = useMemo(() => courses.filter((course) => `${course.title} ${course.status}`.toLowerCase().includes(query.toLowerCase()) && (status === 'All' || course.status === status) && (category === 'All' || course.category === category)), [category, query, status])
	return <InstructorPage title="My Courses" description="Build course content, manage student learning, and submit completed courses for admin review." action={{ label: 'Create course', href: '/instructor/courses/new' }}><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><h2 className="text-sm font-bold text-[#1C1D52]">Course library</h2><p className="mt-1 text-[10px] text-slate-500">You can edit and submit courses, but only admins can publish them.</p></div><span className="rounded-full bg-[#e8f7eb] px-3 py-1 text-[10px] font-bold text-[#397d3a]">{visibleCourses.length} courses</span></div><div className="mt-5 flex flex-col gap-3 rounded-xl bg-[#f8fbff] p-3 md:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg bg-white px-3 py-2 text-[10px] text-slate-400"><Search className="h-3.5 w-3.5" /><span className="sr-only">Search courses</span><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent outline-none" placeholder="Search courses..." /></label><label className="flex items-center gap-2 text-[10px] font-semibold text-slate-500"><Filter className="h-3.5 w-3.5" /><span className="sr-only">Filter by status</span><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 outline-none"><option>All</option><option>Draft</option><option>In review</option><option>Published</option></select></label><label className="text-[10px] font-semibold text-slate-500"><span className="sr-only">Filter by category</span><select value={category} onChange={(event) => setCategory(event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 outline-none"><option>All</option><option>Design</option><option>Development</option></select></label></div><div className="mt-5 space-y-3">{visibleCourses.map((course) => <article key={course.slug} className="flex flex-col gap-4 rounded-xl border border-slate-200 p-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[#dceeff] text-blue-600"><BookOpen className="h-4 w-4" /></span><div><h3 className="text-sm font-bold text-[#1C1D52]">{course.title}</h3><p className="mt-1 text-[10px] text-slate-500">{course.students} students · {course.lessons} lessons · Updated {course.updated}</p></div></div><div className="flex flex-wrap items-center gap-2"><StatusBadge tone={course.tone}>{course.status}</StatusBadge><Link href={`/instructor/courses/${course.slug}/edit`} className="inline-flex items-center gap-1.5 rounded-lg border border-slate-200 px-3 py-2 text-[10px] font-bold text-[#1C1D52]"><Edit3 className="h-3.5 w-3.5" />Edit</Link><Link href={`/instructor/courses/${course.slug}/preview`} className="inline-flex items-center gap-1.5 rounded-lg border border-slate-200 px-3 py-2 text-[10px] font-bold text-[#1C1D52]"><Eye className="h-3.5 w-3.5" />Preview</Link>{course.status === 'Draft' && <button type="button" className="inline-flex items-center gap-1.5 rounded-lg bg-[#5FBB46] px-3 py-2 text-[10px] font-bold text-[#14204f]"><Send className="h-3.5 w-3.5" />Submit review</button>}</div></article>)}</div></section></InstructorPage>
}
