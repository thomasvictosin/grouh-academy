'use client'

import Link from 'next/link'
import { Check, Pencil, Plus, Search, Trash2, X } from 'lucide-react'
import { useEffect, useMemo, useState } from 'react'
import AdminShell from '@/components/AdminShell'

type Course = { id: string; slug: string; title: string; status: string; category?: { name: string } | null; creator?: { name: string | null } | null; instructors: Array<{ instructor: { name: string | null } }>; _count: { enrollments: number } }

export default function AdminCoursesPage() {
  const [courses, setCourses] = useState<Course[]>([])
  const [query, setQuery] = useState('')
  const [status, setStatus] = useState('All')
  const [message, setMessage] = useState<string | null>(null)
  useEffect(() => { void fetch('/api/courses?manage=1').then(async (response) => { if (!response.ok) throw new Error(); return response.json() as Promise<Course[]> }).then(setCourses).catch(() => setMessage('Unable to load courses.')) }, [])
  const visibleCourses = useMemo(() => courses.filter((course) => (status === 'All' || course.status === status) && `${course.title} ${course.category?.name ?? ''}`.toLowerCase().includes(query.toLowerCase())), [courses, query, status])
  async function updateStatus(slug: string, nextStatus: 'PUBLISHED' | 'REJECTED') { const response = await fetch(`/api/courses/${slug}/review`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status: nextStatus }) }); if (!response.ok) { setMessage('Unable to update course status.'); return }; setCourses((items) => items.map((course) => course.slug === slug ? { ...course, status: nextStatus } : course)); setMessage(nextStatus === 'PUBLISHED' ? 'Course published and visible in the catalogue.' : 'Course rejected.') }
  async function removeCourse(course: Course) { if (!window.confirm(`Delete “${course.title}”? This permanently removes the course, curriculum, and enrolments.`)) return; const response = await fetch(`/api/courses/${course.slug}`, { method: 'DELETE' }); if (!response.ok) { setMessage('Unable to delete course.'); return }; setCourses((items) => items.filter((item) => item.id !== course.id)); setMessage('Course deleted.') }
  return <AdminShell workspace="student"><div className="mx-auto max-w-[1400px] space-y-5"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-semibold text-[#1C1D52] sm:text-3xl">All Courses</h1><p className="mt-2 text-xs text-slate-500">Edit course content, publish drafts, and manage the course catalogue.</p></div><Link href="/admin/student/courses/new" className="inline-flex w-fit items-center gap-2 rounded-lg bg-[#5FBB46] px-4 py-2.5 text-xs font-semibold text-[#14204f]"><Plus className="h-4 w-4" />Add New Course</Link></header>{message && <p className="rounded-lg bg-[#e8f7eb] px-4 py-3 text-xs font-semibold text-[#397d3a]" role="status">{message}</p>}<section className="rounded-2xl bg-white p-4 shadow-[0_7px_20px_rgba(28,29,82,0.08)] sm:p-5"><div className="flex flex-col gap-3 border-b border-slate-100 pb-4 sm:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg bg-[#f3f6fb] px-3 py-2.5 text-xs text-slate-400"><Search className="h-3.5 w-3.5" /><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent outline-none" placeholder="Search courses..." /></label><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-[10px] font-medium text-[#1C1D52]"><option>All</option><option>DRAFT</option><option>PENDING_REVIEW</option><option>PUBLISHED</option><option>REJECTED</option><option>ARCHIVED</option></select></div><div className="mt-5 overflow-x-auto"><table className="min-w-[800px] w-full text-left"><thead className="bg-[#f8fbff] text-[10px] uppercase text-slate-500"><tr><th className="px-3 py-3">Course</th><th className="px-3 py-3">Instructor</th><th className="px-3 py-3">Category</th><th className="px-3 py-3">Students</th><th className="px-3 py-3">Status</th><th className="px-3 py-3 text-right">Actions</th></tr></thead><tbody className="text-[11px] text-[#1C1D52]">{visibleCourses.map((course) => <tr key={course.id} className="border-t border-slate-100"><td className="px-3 py-3.5 font-semibold"><Link href={`/admin/student/courses/${course.slug}`}>{course.title}</Link></td><td className="px-3 py-3.5 text-slate-500">{course.instructors[0]?.instructor.name ?? course.creator?.name ?? 'Grouh Academy'}</td><td className="px-3 py-3.5 text-slate-500">{course.category?.name ?? 'Uncategorized'}</td><td className="px-3 py-3.5">{course._count.enrollments}</td><td className="px-3 py-3.5"><span className="rounded-full bg-slate-100 px-2 py-1 text-[9px] font-semibold">{course.status}</span></td><td className="px-3 py-3.5"><div className="flex justify-end gap-1"><Link href={`/admin/student/courses/${course.slug}/edit`} className="inline-flex items-center gap-1 rounded-lg px-2 py-2 font-semibold text-blue-600 hover:bg-blue-50"><Pencil className="h-3 w-3" />Edit</Link>{['DRAFT', 'PENDING_REVIEW', 'REJECTED'].includes(course.status) && <button type="button" onClick={() => void updateStatus(course.slug, 'PUBLISHED')} className="inline-flex items-center gap-1 rounded-lg bg-[#5FBB46] px-2 py-2 font-bold text-[#14204f]"><Check className="h-3 w-3" />Publish</button>}{course.status === 'PENDING_REVIEW' && <button type="button" onClick={() => void updateStatus(course.slug, 'REJECTED')} className="rounded-lg px-2 py-2 text-red-600 hover:bg-red-50"><X className="h-3 w-3" /></button>}<button type="button" onClick={() => void removeCourse(course)} className="rounded-lg px-2 py-2 text-red-600 hover:bg-red-50" aria-label={`Delete ${course.title}`}><Trash2 className="h-3 w-3" /></button></div></td></tr>)}</tbody></table></div>{visibleCourses.length === 0 && <p className="py-12 text-center text-sm text-slate-500">No courses match the current filters.</p>}</section></div></AdminShell>
}
