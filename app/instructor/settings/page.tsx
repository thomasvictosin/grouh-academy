'use client'

import { Bell, Check, CreditCard, Globe2, Lock, Save, Settings2 } from 'lucide-react'
import { useState } from 'react'
import { InstructorPage } from '@/components/InstructorPage'

export default function InstructorSettingsPage() {
  const [settings, setSettings] = useState({ emailNotifications: true, learnerQuestions: true, courseReviews: true, weeklyDigest: false, timezone: 'West Africa Time (WAT)', payoutMethod: 'Bank transfer' })
  const [saved, setSaved] = useState(false)
  const toggle = (field: keyof typeof settings) => setSettings((current) => ({ ...current, [field]: !current[field] }))
  const saveSettings = () => { setSaved(true); window.setTimeout(() => setSaved(false), 2500) }
  return <InstructorPage title="Settings" description="Configure your instructor workspace, notifications, payout preferences, and account security."><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-7"><SettingsHeading icon={<Settings2 className="h-5 w-5" />} title="Account preferences" description="Set the defaults used across your instructor workspace." /><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="block text-xs font-bold text-[#1C1D52]">Workspace timezone<select value={settings.timezone} onChange={(event) => setSettings({ ...settings, timezone: event.target.value })} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-xs font-normal outline-none focus:border-blue-400"><option>West Africa Time (WAT)</option><option>Greenwich Mean Time (GMT)</option><option>Eastern Time (ET)</option></select></label><label className="block text-xs font-bold text-[#1C1D52]">Payout method<select value={settings.payoutMethod} onChange={(event) => setSettings({ ...settings, payoutMethod: event.target.value })} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-xs font-normal outline-none focus:border-blue-400"><option>Bank transfer</option><option>Paystack transfer</option></select></label></div></section><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-7"><SettingsHeading icon={<Bell className="h-5 w-5" />} title="Notification preferences" description="Choose which teaching updates you want to receive." /><div className="mt-5 divide-y divide-slate-100">{[['emailNotifications', 'Email notifications', 'Receive important workspace updates by email.'], ['learnerQuestions', 'Learner questions', 'Get notified when a student asks a question.'], ['courseReviews', 'Course review updates', 'Know when an admin reviews your course submission.'], ['weeklyDigest', 'Weekly teaching digest', 'Receive a weekly summary of course activity.']].map(([field, title, description]) => <div key={field} className="flex items-center justify-between gap-4 py-4"><div><p className="text-xs font-bold text-[#1C1D52]">{title}</p><p className="mt-1 text-[10px] text-slate-500">{description}</p></div><button type="button" onClick={() => toggle(field as keyof typeof settings)} className={`relative h-6 w-11 shrink-0 rounded-full transition ${settings[field as keyof typeof settings] ? 'bg-[#5FBB46]' : 'bg-slate-200'}`} aria-label={`Toggle ${title}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${settings[field as keyof typeof settings] ? 'left-6' : 'left-1'}`} /></button></div>)}</div></section><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-7"><SettingsHeading icon={<CreditCard className="h-5 w-5" />} title="Payout details" description="Your current earnings destination is shown below." /><div className="mt-5 rounded-xl border border-slate-200 bg-[#f8fbff] p-4"><div className="flex items-center gap-3"><CreditCard className="h-5 w-5 text-[#5FBB46]" /><div><p className="text-xs font-bold text-[#1C1D52]">First Bank account ending in 4821</p><p className="mt-1 text-[10px] text-slate-500">Verified for instructor revenue payouts</p></div></div><button type="button" className="mt-4 text-[10px] font-bold text-blue-600">Update payout account</button></div></section><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-7"><SettingsHeading icon={<Lock className="h-5 w-5" />} title="Security" description="Keep your instructor account protected." /><div className="mt-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold text-[#1C1D52]">Password</p><p className="mt-1 text-[10px] text-slate-500">Last updated 42 days ago.</p></div><button type="button" className="rounded-lg border border-slate-200 px-3 py-2 text-[10px] font-bold text-[#1C1D52]">Change password</button></div></section><div className="flex items-center gap-3"><button type="button" onClick={saveSettings} className="inline-flex items-center gap-2 rounded-lg bg-[#5FBB46] px-4 py-2.5 text-xs font-bold text-[#14204f]"><Save className="h-4 w-4" />Save settings</button>{saved && <span className="inline-flex items-center gap-1 text-xs font-semibold text-[#397d3a]"><Check className="h-4 w-4" />Settings saved</span>}</div></InstructorPage>
}

function SettingsHeading({ icon, title, description }: { icon: React.ReactNode; title: string; description: string }) { return <div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[#dceeff] text-blue-600">{icon}</span><div><h2 className="text-sm font-bold text-[#1C1D52]">{title}</h2><p className="mt-1 text-[10px] text-slate-500">{description}</p></div></div> }
