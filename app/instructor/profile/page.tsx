'use client'

import { Award, Camera, Check, Mail, MapPin, Plus, Save, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { InstructorPage } from '@/components/InstructorPage'

export default function InstructorProfilePage() {
  const [profile, setProfile] = useState({ name: 'Sarah Johnson', specialty: 'Product Design & UX Research', location: 'Lagos, Nigeria', bio: 'I help product teams turn research, systems, and prototypes into better digital experiences.' })
  const [credentials, setCredentials] = useState(['Product Design', 'UX Research', 'Design Systems'])
  const [saved, setSaved] = useState(false)
  const update = (field: keyof typeof profile, value: string) => { setProfile((current) => ({ ...current, [field]: value })); setSaved(false) }
  const saveProfile = () => { setSaved(true); window.setTimeout(() => setSaved(false), 2500) }

  return <InstructorPage title="Instructor Profile" description="Manage the professional profile learners see when they discover your courses."><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="relative"><img src="/avatar-placeholder.png" alt="Sarah Johnson" className="h-24 w-24 rounded-full object-cover" /><button type="button" className="absolute bottom-0 right-0 rounded-full bg-[#5FBB46] p-2 text-[#14204f]" aria-label="Change profile photo"><Camera className="h-3.5 w-3.5" /></button></div><div><h2 className="text-xl font-bold text-[#1C1D52]">{profile.name}</h2><p className="mt-1 text-xs text-slate-500">Instructor profile</p><div className="mt-3 flex flex-wrap gap-3 text-[10px] text-slate-500"><span className="flex items-center gap-1"><Mail className="h-3.5 w-3.5" />sarah@grouhacademy.com</span><span className="flex items-center gap-1"><MapPin className="h-3.5 w-3.5" />{profile.location}</span></div></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><Field label="Display name" value={profile.name} onChange={(value) => update('name', value)} /><Field label="Specialty" value={profile.specialty} onChange={(value) => update('specialty', value)} /><Field label="Location" value={profile.location} onChange={(value) => update('location', value)} /></div><label className="mt-4 block text-xs font-bold text-[#1C1D52]">About you<textarea value={profile.bio} onChange={(event) => update('bio', event.target.value)} rows={4} className="mt-2 w-full rounded-lg border border-slate-200 p-3 text-xs font-normal outline-none focus:border-blue-400" /></label><div className="mt-5 flex items-center gap-3"><button type="button" onClick={saveProfile} className="inline-flex items-center gap-2 rounded-lg bg-[#5FBB46] px-4 py-2.5 text-xs font-bold text-[#14204f]"><Save className="h-4 w-4" />Save profile</button>{saved && <span className="inline-flex items-center gap-1 text-xs font-semibold text-[#397d3a]"><Check className="h-4 w-4" />Profile saved</span>}</div></section><section className="rounded-2xl bg-white p-5 shadow-[0_8px_24px_rgba(28,29,82,0.08)] sm:p-6"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[#e8f7eb] text-[#5FBB46]"><Award className="h-5 w-5" /></span><div><h2 className="text-sm font-bold text-[#1C1D52]">Instructor credentials</h2><p className="mt-1 text-xs text-slate-500">Add the expertise learners should see on your profile.</p></div></div><div className="mt-5 flex flex-wrap gap-2">{credentials.map((credential) => <span key={credential} className="inline-flex items-center gap-2 rounded-full bg-[#f3f6fb] px-3 py-2 text-xs font-semibold text-[#1C1D52]">{credential}<button type="button" onClick={() => setCredentials((items) => items.filter((item) => item !== credential))} aria-label={`Remove ${credential}`}><Trash2 className="h-3 w-3 text-slate-400" /></button></span>)}<button type="button" onClick={() => setCredentials((items) => [...items, `Credential ${items.length + 1}`])} className="inline-flex items-center gap-1 rounded-full border border-dashed border-slate-300 px-3 py-2 text-xs font-semibold text-blue-600"><Plus className="h-3.5 w-3.5" />Add credential</button></div></section></InstructorPage>
}

function Field({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="block text-xs font-bold text-[#1C1D52]">{label}<input value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 px-3 text-xs font-normal outline-none focus:border-blue-400" /></label> }
